---
import BaseLayout from '@/layouts/BaseLayout.astro'
import WordleGame from '@/components/WordleGame'
import { COLUMNS } from '@/lib/config'
import { getNewWord } from '@/lib/wordle'

// const response = await fetch(`https://random-word-api.herokuapp.com/word?number=1&length=${COLUMNS}`)
// const words = await response.json()

const word = await getNewWord()
---

<BaseLayout title="Playful" description={`Wordle!`}>
  <WordleGame wordFromAPI={word} client:load />
</BaseLayout>
